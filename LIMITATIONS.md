# FinTrace-CN 限制与合规边界

- 仓库自带 Demo 是明确标记的 illustrative 数据，不是真实价格或财报。
- 本地真实快照可能陈旧，历史快照不会被显示为实时行情。
- 在线 Provider 受凭据、数据权限、限流、网络和上游 Schema 影响。
- Validator 只证明已捕获输入与规则通过检查，不保证数据源绝对正确，也不代表适合投资。
- 当前估值只覆盖 PE、PB、PS；EV/EBITDA 与 DCF 不属于已验证主线。
- Agent 评测重点覆盖工具、参数、证据与失败阻断，不能代表完整研究质量。
- dry-run/mock 指标不能作为真实模型效果；真实 LLM 输出仍可能随服务端模型版本变化。
- 本地 JSON 任务与线程池适合单机 Demo，不是多实例生产任务队列。
- 系统不执行交易、不连接券商账户、不提供个性化投资建议。
