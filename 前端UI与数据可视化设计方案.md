# FinTrace-CN 前端 UI 与数据可视化设计方案

## 1. 目标与原则

下一阶段目标不是单纯将 CLI 套一层界面，而是建设“可验证研究工作台”：用户能快速阅读公司状态、理解估值、查看 Agent 做了什么，并在任一关键数字处回到数据来源与校验结果。

设计优先级：**可信度可见 > 研究效率 > 信息密度 > 视觉装饰**。所有数字和图表必须标示数据状态（快照/实时）、研究截止时间、单位、币种、数据源与 Evidence ID；不允许用颜色或文案暗示未经验证的买卖建议。

## 2. 用户与核心任务

| 用户 | 核心任务 | 关键界面 |
|---|---|---|
| 研究者 | 输入公司，快速形成可复核研究结论 | 研究总览、报告、证据抽屉 |
| 分析/开发人员 | 排查 Agent 工具调用、失败与时点问题 | 执行轨迹、校验中心、原始 JSON |
| 演示评审者 | 直观看到系统为何可信、与普通问答有何不同 | 估值页、证据链、消融指标 |

## 3. 信息架构

```text
工作台
├── 研究总览
├── 市场与行情
├── 财务表现
├── 同行估值
├── 证据与校验
├── Agent 执行轨迹
├── 中文研究报告
└── 评测与实验
```

页面顶部固定显示：公司名称与代码、研究截止时间、快照 ID、数据状态、校验状态，以及“导出 Markdown / Excel / 元数据”操作。若报告依赖历史快照，使用中性提示明确“非实时行情”。

## 4. 页面与可视化设计

### 4.1 研究总览

第一屏以“研究卡片 + 关键趋势 + 可信度状态”替代营销型大屏。

- 公司卡：名称、代码、行业、实体类型、数据状态、研究截止时间；
- 市场卡：RAW 收盘价、市值、TTM P/E，并附 Evidence ID 快捷入口；
- 校验卡：通过/未通过、错误数、缺失证据数、时点检查状态；
- 核心趋势：价格区间与收入/归母净利润的双轴趋势（单位独立显示，避免误导）；
- 研究摘要：仅展示 Validator 已允许的结论；未通过时展示阻断原因和补救路径。

### 4.2 市场与行情

- K 线或收盘价折线：RAW、QFQ、HFQ 口径切换，但默认 RAW；
- 成交量与价格联动缩放；
- 研究时点垂线和快照覆盖区间；
- 悬浮提示中显示交易日、价格、口径、来源和 Evidence ID；
- 不以红绿颜色单独表达涨跌，增加文字和图形提示以兼顾可访问性。

### 4.3 财务表现

- 收入、归母净利润、利润率的年度/季度趋势；
- FY / Q1 / H1 / 9M / TTM 切换，展示期间计算说明；
- 财务指标表支持“查看证据”展开，显示报告期、披露时间、单位、字段路径；
- 缺失指标使用“未覆盖”而非零值，禁止图表自动补值。

### 4.4 同行估值

- PE / PB / PS 分布箱线图或点阵图，目标公司突出显示；
- 同行表展示入选理由、期间、数据状态、异常值标识和剔除理由；
- 倍数到隐含市值的瀑布图，清楚区分原始输入与计算结果；
- 金融机构自动隐藏不适用方法，并给出可读的适用性提示；
- 置信等级由覆盖数、异常值和证据完整度驱动，不能由模型自由生成。

### 4.5 证据与校验中心

这是 FinTrace-CN 的差异化核心。

- Evidence Ledger 表：ID、指标、数值、期间、来源、披露时间、单位、状态；
- 点击数字或图表数据点，侧边抽屉定位到对应 Evidence ID；
- 计算依赖关系以小型有向图展示“原始事实 → TTM → 市值 / 倍数”；
- Validator 清单按通过、警告、阻断分组，逐条显示规则、受影响指标和修复建议；
- 提供原始 JSON 查看和复制，但不允许前端直接修改证据。

### 4.6 Agent 执行轨迹

- 时间线显示计划、工具调用、返回状态、证据数、耗时与成本；
- 工具参数与返回摘要默认折叠，避免暴露冗长原始数据；
- 失败节点以“失败原因—是否重试—降级路径—最终状态”展示；
- 将外部工具内容继续标记为数据，前端不得将其渲染为可执行 HTML 或指令。

### 4.7 评测与实验

- 展示基准版本、快照版本、模型、温度、最大步数和 Git 提交；
- 四组消融以并列条形图展示工具路由、参数准确率、证据覆盖、结论泄漏和端到端成功率；
- 指标缺失必须明确标记“未测量”，不能用 0 或插值冒充实验结果；
- 支持从每个聚合指标下钻到案例和完整 `ResearchState`。

## 5. 交互与状态规范

| 状态 | 呈现 | 行为 |
|---|---|---|
| 已验证快照 | 蓝灰状态标签 + 快照 ID | 可查看证据、导出与复现 |
| 在线数据 | 明确 Provider 与获取时间 | 提示覆盖和授权限制 |
| 校验通过 | 绿色标签 + 规则摘要 | 展示允许的受约束结论 |
| 校验警告 | 琥珀色标签 | 允许查看，但突出风险与缺口 |
| 校验阻断 | 红色标签 | 隐藏确定性结论，展示失败原因 |
| 数据缺失 | 空状态而非 0 | 提供重新运行或补充快照入口 |

## 6. 技术实施建议

首期建议采用前后端分离：前端使用 React + TypeScript，图表使用 ECharts；后端以轻量 API 适配现有 Python 领域对象，不在 UI 层复制财务计算。若目标是快速演示，可先实现只读 HTML/Streamlit 原型，但其接口和状态模型应与正式 Web API 保持一致。

建议新增以下只读接口：

| 接口 | 返回内容 |
|---|---|
| `GET /api/research/{id}/summary` | 总览、数据状态、校验状态与关键指标 |
| `GET /api/research/{id}/market` | RAW/QFQ/HFQ 行情、口径和证据映射 |
| `GET /api/research/{id}/financials` | 期间化财务序列与元数据 |
| `GET /api/research/{id}/valuation` | 同行、倍数、异常值、计算结果与置信等级 |
| `GET /api/research/{id}/evidence` | Evidence Ledger 与计算依赖 |
| `GET /api/research/{id}/validation` | 校验规则、状态、错误与阻断原因 |
| `GET /api/research/{id}/trace` | ResearchPlan、ToolTrace、成本和延迟 |
| `POST /api/research` | 启动受控研究任务；请求只接受已定义 schema |

接口应该直接序列化经验证的领域结果，附带 `snapshot_id`、`research_as_of`、`currency`、`unit`、`period_basis`、`evidence_ids` 和 `validation_status`。前端不自行推导 TTM、估值或校验结论。

## 7. 分期计划与验收标准

### 阶段 A：可点击原型（约 1 周）

- 完成研究总览、行情、财务、证据抽屉四个只读页面；
- 只接入 `output/demo/600519` 的固定产物；
- 验收：每项首页关键数值可在两次点击内定位 Evidence ID，页面明确显示快照和研究时点。

### 阶段 B：研究工作台 MVP（约 2 周）

- 建立上述只读 API，支持多个研究产物；
- 加入同行估值、Validator 中心、Agent 轨迹和导出；
- 验收：报告、图表和 Excel 中的关键数字一致；校验阻断时 UI 不显示确定性投资结论。

### 阶段 C：受控任务与评测看板（约 1–2 周）

- 支持发起研究、进度更新、失败恢复和任务历史；
- 加入基准/消融对比与案例下钻；
- 验收：任务参数、模型版本、快照、工具轨迹和成本完整可追溯，前端状态与 `ResearchState` 一致。

## 8. 风险与边界

- 不把前端作为新的计算来源；所有财务与估值计算仍在后端确定性模块完成。
- 不将 API Key、原始付费数据和完整受版权保护文件发送到浏览器。
- 不在首期引入实时交易、个性化推荐或复杂多租户权限系统。
- 不为图表“补全”缺失数据；图形漂亮不能替代数据可用性和证据完整性。
