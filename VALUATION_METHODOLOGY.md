# FinTrace-CN 估值方法论

## 确定性输入

同行估值由 Python 基于已登记证据的快照输入计算：RAW 价格、总股本、TTM 归母净利润、账面权益、营业收入、报告口径及固定行业分类。LLM 不计算也不可改写这些数值。

## 已支持倍数

当前同行工作流支持 PE、PB 和 PS。系统记录同行入选原因，以 IQR 规则处理异常值，并在同行覆盖不足时降低置信等级。

- PE 剔除非正利润样本；
- PB 使用账面权益，并受实体类型适用性检查；
- PS 要求目标与同行使用相同期间基础的营业收入。

银行及其他金融机构受估值适用性矩阵约束，系统不会静默改用不适合的企业价值倍数。

## 期间与单位控制

`FinancialPeriodEngine` 统一 FY、Q1、H1、9M 与 TTM。A 股累计中报和三季报会先依确定性规则转换，再用于 TTM 指标。Validator 检查单位、币种、期间基础、缺失输入与公式一致性。

## 当前范围

EV/EBITDA 及 DCF 交叉校验仍是后续工作。仓库保留的 Excel DCF 能力独立存在；在未携带同等证据与 Validator 约束前，不应将其表述为 FinTrace-CN 的可验证同行估值结果。

本软件仅用于研究辅助，不构成投资建议。
