# Stock Analyst Pipeline Requirements
#
# Usage:
#   pip install -r requirements.txt         # core + AI + default CN
#   pip install -r requirements-full.txt    # optional: vynn-core, yfinance
#
# Core dependencies — always required
PyYAML>=6.0.2
python-slugify>=8.0.4
newspaper3k>=0.2.8
google-search-results>=2.4.2
lxml_html_clean>=0.4.2

# AI/LLM functionality
openai>=1.30.0
anthropic>=0.65.0
tiktoken>=0.9.0

# Financial data scraping — A-share (required)
pandas>=2.0.0
numpy>=1.24.0
tushare>=1.4.29
akshare>=1.18.0

# Excel export
openpyxl>=3.1.0

langgraph>=0.3.14
requests>=2.31.0

# ---------------------------------------------------------------------------
# Optional dependencies — install only when needed (pip install -r requirements-full.txt)
# ---------------------------------------------------------------------------
# vynn-core — News feed data layer for MongoDB/Redis (legacy; not needed for CN)
#   vynn-core @ git+https://github.com/Agentic-Analyst/vynn-core.git@7bd5b3e736ee6d5dc4df1347dac4b80390f8f338
# yfinance — Yahoo Finance (legacy US equities; not in A-share default path)
#   yfinance>=0.2.28
