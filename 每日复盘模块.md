# FinTrace-CN「A股每日复盘」模块

> 离线优先、可验证的 A 股每日市场复盘能力。覆盖「全景复盘」（市场整体）与「热点复盘」（板块 / 资金 / 涨停），所有关键数字写入 Evidence Ledger，结论经确定性闸门（gate）校验。

## 1. 模块定位

每日复盘复用既有 Provider / Evidence / Validator / Research Gate 基础设施，构建一套**默认离线、可复现、可审计**的市场复盘流程。它不把大模型生成的数字当作事实，也不把一次「看起来合理」的盘面描述当作研究结论；每项关键数字都必须能追溯到快照来源、研究时点和公式。

模块当前聚焦研究辅助，**不提供个性化投资建议、不连接券商账户、不执行交易、不预测投资收益**。

## 2. 数据口径（caliber）

### 2.1 快照结构

市场复盘快照与个股快照**同构**（isostructural）：

```text
schema_version / snapshot_id / provider / symbol / research_as_of / fetched_at
/ data_quality / source_metadata / data{ ... }
```

模块专属常量：

| 常量 | 取值 | 说明 |
|---|---|---|
| `SCHEMA_VERSION` | `market-review-1.0.0` | 市场复盘快照 schema 版本 |
| `MARKET_SYMBOL` | `CN_MARKET` | 市场快照固定标的（区别于个股 `600519.SH` 等） |
| `SYNTHETIC_PROVIDER` | `synthetic_demo` | 离线演示快照 provider |
| `SYNTHETIC_DATA_QUALITY` | `illustrative_test_fixture_not_for_research` | 离线演示快照数据质量标记 |

### 2.2 字段口径

- **指数行情**（`data.indices`）：收盘点位、涨跌幅、成交额等；计算基于 **RAW（不复权）** 口径，与个股估值口径保持一致。
- **涨跌家数 / 涨停家数**（`data.breadth`）：市场宽度口径，作为 gate 的关键覆盖检查项。
- **板块排行**（`data.sectors`）、**资金流向**（`data.flows`）、**涨停梯队**（`data.limit_up`）：来自快照 `data` 字段，单位与快照内标注一致。
- **研究时点**：`research_as_of = 复盘日期`；`fetched_at = 快照生成时间`。
- **证据链**：所有关键数字写入 Evidence Ledger（`evidence_id` / `provider` / `as_of` / `unit` / `field_path` / `depends_on`），可被前端证据抽屉回溯。

## 3. 实现路径（MVP）

| 路径 | 入口 | 章节数 | 覆盖 |
|---|---|---|---|
| 全景复盘 `panorama` | `daily_review_detail(id, "panorama")` | 8 章 | 指数收盘、市场特征、个股动态、核心驱动、领涨领跌、偏离复盘、次日推断、宏观快照 |
| 热点复盘 `hotspots` | `daily_review_detail(id, "hotspots")` | 6 章 | 市场概览、板块排行、热门板块、成交额榜、净流入榜、10 日涨幅榜 |

**未覆盖章节一律明确标注「未覆盖」，绝不编造数字**：

- `panorama` 中的「次日推断 `next_day_inference`」「宏观快照 `macro_snapshot`」标注为未覆盖；
- `hotspots` 中的「板块 5 日 `sector_5d`」「10 日涨幅榜 `top_10d_gainers`」标注为未覆盖。

## 4. 边界与安全（boundaries）

- **离线优先**：默认走本地版本化快照，演示、测试与回归无需网络、代理或 API Key。
- **实时失败回退**：`collect_daily_review(..., live=True)` 的 Tushare 实时采集若失败，一律回退到**明确标注 `synthetic_demo`** 的快照——`source_metadata.synthetic_demo = true`、`provider = "synthetic_demo"`、`data_quality = "illustrative_test_fixture_not_for_research"`。**绝不冒充实时行情**。
- **闸门（gate）**：`review_gate(snapshot)` 是确定性阻断闸门：
  - 缺失关键指数或时间戳 → `blocked`；
  - 快照为 `synthetic_demo` → `warning`（不阻断，可用于演示，但不可用于实盘）；
  - 其余 → `pass`。
- **结论可信度**：即便 gate 通过，也仅表示输入与既定规则在固定口径下通过检查，不代表任何证券适合任何投资者，亦不保证未来收益。

## 5. 目录与文件

```text
src/cn/daily_review/
├── schema.py       # MarketReviewSnapshot 及子结构；from_payload/to_payload；is_synthetic_demo
├── provider.py     # DailyReviewSnapshotProvider（镜像 SnapshotProvider）
├── collector.py    # collect_daily_review(_build_synthetic/_build_live)；write_market_snapshot
├── analysis.py     # panorama_review（8 章）/ hotspot_review（6 章）
└── gate.py         # review_gate → GateResult（pass/warning/blocked）
src/cn/workbench_service.py   # load_market_review / list_daily_reviews / daily_review_summary
                            # / daily_review_detail / start_daily_review
api.py                      # GET /api/daily-review, /{id}/summary, /panorama, /hotspots
                            # POST /api/daily-review/acquire (202)
workbench.py                # 「每日复盘」页面（指数卡 / 板块柱图 / 涨停梯队 / 资金流 / 结论 / 证据抽屉）
scripts/collect_daily_review.py   # CLI：--review-date / --live / --token / --output-dir
data/snapshots/market/MARKET_20260814_synthetic_demo_v1.json   # 离线演示快照
tests/test_cn_daily_review.py               # 11 个单元（schema/provider/analysis/gate）
tests/test_daily_review_service.py          # 13 个 service + API 合约
```

## 6. 如何运行

### 6.1 离线 CLI 生成快照

```powershell
.\.venv\Scripts\python.exe scripts\collect_daily_review.py --review-date 20260814
# 实时（需 TUSHARE_TOKEN，失败自动回退 synthetic_demo）：
.\.venv\Scripts\python.exe scripts\collect_daily_review.py --review-date 20260814 --live --token $env:TUSHARE_TOKEN
```

### 6.2 服务层 / API

```python
from src.cn import workbench_service as svc
svc.list_daily_reviews()                                   # 列出可用复盘快照
svc.daily_review_summary("MARKET_20260814_synthetic_demo_v1")
svc.daily_review_detail("MARKET_20260814_synthetic_demo_v1", "panorama")
svc.daily_review_detail("MARKET_20260814_synthetic_demo_v1", "hotspots")
svc.start_daily_review("20260814")                          # 离线生成并落盘快照，返回任务
```

API（启动 `uvicorn api:app` 后）：

```text
GET  /api/daily-review
GET  /api/daily-review/{id}/summary
GET  /api/daily-review/{id}/panorama
GET  /api/daily-review/{id}/hotspots
POST /api/daily-review/acquire   # {"review_date": "20260814"} → 202
```

### 6.3 Streamlit 工作台

```powershell
.\.venv\Scripts\python.exe -m streamlit run workbench.py
```

左侧导航选择「每日复盘」，即可查看指数概览、板块排行、涨停梯队、资金流向、全景/热点结论与证据抽屉。

## 7. 测试

```powershell
.\.venv\Scripts\python.exe -m pytest -q -m "not integration" -p no:cacheprovider
```

每日复盘相关测试位于 `tests/test_cn_daily_review.py` 与 `tests/test_daily_review_service.py`，覆盖：离线标记、schema 拒绝、provider 载入、全景 8 章 / 热点 6 章、gate 对缺失指数/时间戳的阻断、gate 对 synthetic 的 warning、确定性复现等。

## 8. 免责声明

本项目仅用于金融研究、工程实践与教育目的，不构成证券、基金或任何金融产品的投资建议。离线 `synthetic_demo` 快照为演示固定样本，**并非实时行情**；即使 Validator / Gate 通过，也仅表示已捕获输入和规则在既定口径下通过检查，不代表证券适合任何投资者，亦不保证未来收益。
